import React from 'react';
import "./Dialog.less";

const Dialog = ({ resolve, initialValue = 0 }) => {//content, params
  return (
    <div className="prb-dialog">
      dialog
    </div>
  );
};

export default Dialog;
